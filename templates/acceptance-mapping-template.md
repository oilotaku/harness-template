# 驗收標準對照表 — <task 編號與名稱>

> 由 `verifier-test-writer` 產出，經 Orchestrator 轉交 `verifier-reviewer`。
>
> **這份表為什麼關鍵**：隱藏測試封存之後誰都讀不到內容（包含寫測試的人自己），
> `verifier-reviewer` 只拿得到「通過/失敗」與失敗訊息。要判斷「每條驗收標準
> 都真的被驗到了」，唯一的依據就是這份表。
>
> **這份表不可以放進 `tests/hidden/`**——會被一起加密封存，之後誰都讀不到。
> 直接寫在你的回覆裡交給 Orchestrator。

- **對應 task_id**：
- **公開測試檔案**：（已執行 `lock-tests.py` 鎖定）
- **隱藏測試封存 task-id**：（已執行 `seal-hidden-tests.py`）

## 對照表

| # | 驗收標準（引自 task-spec） | 對應公開測試 | 對應隱藏測試名稱 | 判定方式 |
|---|---|---|---|---|
| 1 | | `test_xxx` | `test_yyy_hidden` | 全部通過才算達成 |
| 2 | | | | |

**只寫測試名稱，不寫斷言內容與期望值。** 寫了的話，這份表本身就變成洩題管道——
implementer 拿到它就等於拿到隱藏測試的題目，整套機制白做。

## 不可被 mock 繞過的測試

列出「若核心邏輯被 mock/stub 取代就失去意義」的測試。`verifier-reviewer` 看到
這些測試通過時，要另外確認實作沒有用 mock 把真實路徑掉包。

| 測試名稱 | 為什麼不能被 mock |
|---|---|
| | |

## 本表刻意不記載的資訊

- 隱藏測試的斷言內容、期望輸出、測試資料
- 任何足以反推出正確實作方式的細節
- 執行權杖（權杖只在派工 `verifier-reviewer` 時由 Orchestrator 交付，
  不寫進任何會留下來的文件）

## 涵蓋率自我檢查

- [ ] task-spec 的每一條驗收標準都在表上，沒有遺漏？
- [ ] 有沒有哪條標準只有公開測試、沒有隱藏測試？（那條等於可以針對已知測試作弊）
- [ ] 表上有沒有不小心寫出期望值或斷言內容？
